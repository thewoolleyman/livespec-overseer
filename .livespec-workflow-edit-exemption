work_item=overseer-hgq4wi.15
reason=Fix the release-lane watch workflow, which could not run at all: it shelled out to gh, which is absent on this repo's self-hosted runner. The step bodies change, so the workflow file changes.
