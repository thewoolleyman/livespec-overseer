work_item=overseer-hgq4wi.10
reason=Arm the LLOC soft-band lever in PR CI so a file crossing into the band without an owner marker fails its own pull request instead of redding the release lane after merge. Requires editing the CI workflow.
