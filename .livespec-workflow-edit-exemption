# Per-change workflow edit exemption. One exemption, one reviewed change.
# Consumed by scripts/check-no-workflow-edits.sh, which requires this file to be
# authored in THIS branch's own diff rather than inherited from master.
work_item=livespec-citqsd
reason=Retire the zero-.py CI gate skip per plan pr-gate-master-parity R3 — PR gate == master gate; attended maintainer-authorized workflow edit. Deletes the setup/detect-py-changes job and every gating job's `needs: setup` + `py_changed` step conditions, and relocates check-ci-gate-parity into check-metadata-batch with LIVESPEC_FAIL_IF_CI_GATE_PARITY_GAPS_EXIST armed. Edits step/job/needs lines, outside the mechanical pin/reconciler allowance, so declared here.
