work_item=overseer-bnutz7
reason=Carry the coverage provenance marker in the CI artifact and let both coverage jobs resolve the run id themselves, so check-coverage reuses the producer's data across the job boundary without trusting aggregate-local state, and rename the marker out of coverage's parallel-data namespace.
